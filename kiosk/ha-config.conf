# Home Assistant Configuration
# Update these values for your specific setup

# Primary Home Assistant URL (will be tried first)
HA_PRIMARY_URL="http://localhost:8123"

# Fallback URLs (will be tried if primary fails)
HA_FALLBACK_URLS=(
    "http://localhost:8123"
    "http://127.0.0.1:8123"
)

# Timeout for connection attempts (seconds)
HA_TIMEOUT=5

# Retry interval (seconds)
HA_RETRY_INTERVAL=5 